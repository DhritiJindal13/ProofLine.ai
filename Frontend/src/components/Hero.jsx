function Hero() {
  return (
    <header className="screen-heading source-heading">
      <div>
        <span className="eyebrow">SOURCES</span>
        <h1 id="sources-title">Match your resume against the role.</h1>
      </div>
      <p>
        Find the evidence that matches the job, expose real gaps, and rewrite weak bullets without inventing experience.
      </p>
    </header>
  );
}

export default Hero;
